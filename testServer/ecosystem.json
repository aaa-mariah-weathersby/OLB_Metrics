{
  apps: [
    {
      name: "testServer",
      script: "server.js",
      env: {
        COMMON_VARIABLE: "true"
      },
      env_test: {
        NODE_ENV: "test",
        PORT: 5400,
      },
    },
  ],
  deploy: {
    test: {
      user: "root",
      host: "10.14.254.21",
      ref: "origin/test",
      repo: "git@github.com:clublabs/AppDeploy.git",
      path: "/var/www/apptest",
      "post-deploy": "npm install && pm2 startOrRestart ecosystem.json --env test",
      env: {
        NODE_ENV: "test"
      }
    },
  }
}